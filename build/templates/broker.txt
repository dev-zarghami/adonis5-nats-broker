// import Broker from '@ioc:Adonis/Addons/NatsBroker'

// Core NATS request/reply routes
// Broker.middleware(['userAuth'])
// Broker.route('get.users', 'UserController.index')
// Broker.route('post.users', 'UserController.store')
// Broker.route('put.users.{id}', 'UserController.update')
// Broker.route('get.users.{id}', 'UserController.show')
// Broker.route('delete.users.{id}', 'UserController.destroy')
// Broker.middleware()

// Grouped routes — shared prefix + middleware; chain .middleware() per route
// Broker.group(() => {
//   Broker.route('get.users', 'UserController.index')
//   Broker.route('get.users.{id}', 'UserController.show').middleware('audit')
// })
//   .prefix('admin')
//   .middleware('auth')

// JetStream consumers (same controller style)
// Enable jetstream in config/nats.ts and run `node ace nats:sync` first.
// import Jet from '@ioc:Adonis/Addons/NatsJetStream'
//
// Jet.consume(
//   { stream: 'ORDERS', durable: 'orders-worker', filterSubject: 'orders.created' },
//   'OrdersController.onCreated'
// )
